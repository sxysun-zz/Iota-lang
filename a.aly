(+ 13 (- 32 (* 5 1)))
//(= x 1)
//(> x 2)
/*
(defun max
      (lambda (m)
            (lambda (n)
                  (if (> n m)
                        n
                        m))))
(max (+ 1 1) (* 2 3))
*/
//(≥ x 1)