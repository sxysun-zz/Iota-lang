(+ 13 (- 32 (* 5 1)))
//(lambda (x) (+ x x))
//(= x 1)
//(> x 2)
/* \greaterOrEqual \equiv
(defun max
      (lambda (m)
            (lambda (n)
                  (if (> n m)
                        n
                        m))))
(max (+ 1 1) (* 2 3))
*/
//(≥ x 1)