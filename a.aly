(+ 13 (- 32 (* 5 1)))

/*
(defun max
      (lambda (m)
            (lambda (n)
                  (if (> n m)
                        n
                        m))))
(max (+ 1 1) (* 2 3))
*/
