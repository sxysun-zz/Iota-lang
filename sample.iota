(= x 1)
(= y 32.0)
(+ 13 (- y (* 5 x)))
//(lambda (x) (+ x x))
//(= x 1)
//(> x 2)
/* \ge \equiv
(defun max
      (lambda (m)
            (lambda (n)
                  (if (> n m)
                        n
                        m))))
(max (+ 1 1) (* 2 3))
*/
//(≥ x 1)